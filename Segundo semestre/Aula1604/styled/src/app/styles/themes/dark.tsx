import { DefaultTheme } from "styled-components";

const dark : DefaultTheme = {
    title: "dark",
    colors: {
        primary: "#333",
        secondary: "#1a1a1a",
        textColor: "#ccc"
    }
}

export default dark;